; Exercises: systemd accumulates repeated keys into a list (duplicate_keys = accumulate)
[Service]
Environment=HOME=/home/user
Environment=PATH=/usr/bin:/bin
Environment=DEBUG=1
ExecStart=/usr/bin/myservice
